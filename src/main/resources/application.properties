# H2 in-memory datasource
spring.datasource.url=jdbc:h2:mem:fintechdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 console (handy for local inspection)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Let Hibernate create the schema, then run data.sql
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Run data.sql after Hibernate has created the tables
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
